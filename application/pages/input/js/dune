(executable
  (name       page_input)
  (libraries  components_lab page_input_tyxml)
  (preprocess (pps js_of_ocaml-ppx)))

(install
  (files (page_input.bc.js as js/page-input.js))
  (section share)
  (package ui-server))