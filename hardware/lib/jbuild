(jbuild_version 1)

(library
 ((name        hardware)
  (public_name hardware)
  (wrapped false)
  (modes (native))
  (synopsis "hardware implementation")
  (ocamlopt_flags (:standard -short-paths -strict-sequence -bin-annot
                   -thread -w "+1..49+60" -w "+a-4-7-23-33-39-40-41-42-44-45-48"
                   -O3 -unbox-closures))
  (libraries (ctypes ctypes.foreign lwt lwt.preemptive
              websocket-lwt.cohttp lwt_react
              common meta_board board-dvb-niit
              board-ip-dektec board-qos-niit))
  (preprocess (pps (ppx_deriving_yojson ppx_sqlexpr ppx_cstruct)))))
