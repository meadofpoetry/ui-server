(* TODO refactor *)
module CSS = Components_tyxml.CSS

module Avatar = Avatar

module Box = Box

module Vbox = Vbox

module Hbox = Hbox

module Button = Button

module Card = Card

module Circular_progress = Circular_progress

module Clusterize = Clusterize

module Checkbox = Checkbox

module Material_color_palette = Components_tyxml.Material_color_palette

module Dashboard = Dashboard

(* TODO add focus on 'accept' action on show *)
module Dialog = Dialog

module Divider = Divider

module Drawer = Drawer

module Dynamic_grid = Dynamic_grid

module Elevation = Elevation

module Expansion_panel = Expansion_panel

module Fab = Fab

(* TODO fix show bugs *)
module Fab_speed_dial = Fab_speed_dial

module Form_field = Form_field

module Hexdump = Hexdump

module Icon = Icon

module Icon_button = Icon_button

module Layout_grid = Layout_grid

(* (\* TODO remove js dependency *\)
 * module Linear_progress = Linear_progress *)

module Item_list = Item_list

module Infinite_scroll = Infinite_scroll

(* TODO remove js dependency *)
module Menu = Menu

module Pagination = Pagination

(* TODO add group, fix selection signal *)
module Radio = Radio

module Ripple = Ripple

module Scaffold = Scaffold

(* TODO update styles *)
module Select = Select

module Side_sheet = Side_sheet

(* TODO remove js dependency *)
(* module Slider = Slider *)

(* TODO remove js dependency *)
(* XXX never used *)
(* module Snackbar = Snackbar *)

module Vsplit = Vsplit

module Hsplit = Hsplit

module Switch = Switch

module Table = Table

module Tab = Tab
module Tab_indicator = Tab_indicator
module Tab_scroller = Tab_scroller
module Tab_bar = Tab_bar

module Textfield = Textfield

(* TODO add fixed and short top app bars *)
module Top_app_bar = Top_app_bar

(* TODO add lines? *)
module Tree = Tree

(* TODO refactor *)
module Touch_draggable = Touch_draggable

module Typography = Typography

module Utils = Utils

(* Base classes *)
module Widget = Widget
