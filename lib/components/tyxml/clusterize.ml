module CSS = struct
  let root = "mdc-clusterize"
  let extra_row = BEM.add_element root "extra-row"
  let top_space = BEM.add_modifier root "top-space"
  let bottom_space = BEM.add_modifier root "bottom-space"
end
