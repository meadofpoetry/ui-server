(library
  (name        sender)
  ;(public_name sender)
  (modes       native)
  (synopsis   "sender implementation")
  (libraries  containers yojson caqti-lwt cohttp angstrom
              lwt storage process))
 ;; (preprocess (pps (ppx_deriving_yojson)))))
