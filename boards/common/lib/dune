(library
  (name        boards)
  (synopsis    "boards common")
  (libraries   lwt containers containers.data yojson
               common storage api cstruct ctypes)
  (preprocess (pps ppx_deriving.eq ppx_deriving_yojson)))
