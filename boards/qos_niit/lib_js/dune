(library
  (name        board_qos_niit_js)
  (synopsis    "js interface of the qos_niit board")
  (libraries   containers common api_js components gen_js_api boards_js ui_templates
               chartjs chartjs.datalabels ptime.clock.jsoo)
  (preprocess  (pps ppx_deriving_yojson ppx_deriving.eq ppx_deriving.show ppx_deriving.ord
               js_of_ocaml-ppx)))

(rule
  (targets board_types.ml)
  (deps (:x ../board_types.ml))
  (action (copy %{x} %{targets})))

(rule
  (targets ts_error.ml)
  (deps (:x ../ts_error.ml))
  (action (copy %{x} %{targets})))