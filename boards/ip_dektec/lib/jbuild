(jbuild_version 1)

(library
 ((name        board_ip_dektec)
  (public_name board-ip-dektec)
  (wrapped false)
  (synopsis "ip board implementation")
  (libraries (containers lwt_react websocket-lwt.cohttp common storage meta_board api))
  (preprocess (pps (ppx_cstruct ppx_deriving_yojson ppx_sqlexpr)))))

(rule
 ((targets (board_types.ml))
  (deps (../board_types.ml))
  (action (copy ${<} ${@}))))
