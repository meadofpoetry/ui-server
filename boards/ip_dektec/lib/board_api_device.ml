open Containers
open Common
open Board_types
open Board_protocol
open Board_api_common
open Api.Interaction
open Api.Redirect

(** API
    POST /device/reset
    POST /device/ip
    POST /device/mask
    POST /device/gateway
    POST /device/dhcp

    GET  /device/ip
    GET  /device/mask
    GET  /device/dhcp
    GET  /device/config
    GET  /device/state
    GET  /device/info
 *)

module WS = struct

end

module HTTP = struct

end
