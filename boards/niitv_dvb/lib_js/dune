(library
  (name        board_dvb_niit_js)
  (synopsis    "js interface of the dvb_niit board")
  (libraries   containers common api_js components boards_js ui_templates
               chartjs chartjs.datalabels chartjs.streaming)
  (preprocess  (pps ppx_deriving_yojson js_of_ocaml-ppx ppx_deriving.eq
                ppx_deriving.show ppx_deriving.enum)))

(rule
  (targets board_types.ml)
  (deps    (:x ../board_types.ml))
  (action  (copy %{x} %{targets})))
