(jbuild_version 1)

(library
 ((name        meta_board)
  (public_name meta_board)
  (wrapped false)
  (synopsis "common board description and related types")
  (libraries (containers containers.data lwt react common storage api cbuffer))))
