(jbuild_version 1)

(library
 ((name        board)
  (public_name board)
  (wrapped false)
  (synopsis "common board description and related types")
  (libraries (containers lwt react common storage api cbuffer))))
