(library
  (name        board_dvb_niit)
  (synopsis    "dvb board implementation")
  (libraries   containers websocket-lwt.cohttp common
               storage boards api ptime.clock.os)
  (preprocess (pps ppx_cstruct ppx_deriving_yojson ppx_deriving.eq
               ppx_deriving.show ppx_deriving.enum)))

(rule
  (targets board_types.ml)
  (deps    (:x ../board_types.ml))
  (action  (copy %{x} %{targets})))
