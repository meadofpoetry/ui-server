(library
  (name        board_ts2ip_niit)
  (synopsis    "ts2ip converter board implementation")
  (libraries   containers websocket-lwt.cohttp common storage boards api bitstring)
  (preprocess  (pps ppx_cstruct bitstring.ppx ppx_deriving.eq
               ppx_deriving.show ppx_deriving_yojson)))

(rule
  (targets board_types.ml)
  (deps    (:x ../board_types.ml))
  (action  (copy %{x} %{targets})))
