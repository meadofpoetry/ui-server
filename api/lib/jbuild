(jbuild_version 1)

(library
 ((name        api)
  (public_name api)
  (modules (handler authorize interaction redirect msg_conv))
  (synopsis "api framework")
  (libraries (containers cohttp cohttp.lwt yojson msgpck))))
