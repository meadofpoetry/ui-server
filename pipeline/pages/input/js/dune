(executable
  (name       pipeline_page_input)
  (libraries  pipeline_widgets)
  (preprocess (pps js_of_ocaml-ppx)))

(install
  (files (pipeline_page_input.bc.js as js/pipeline-page-input.js))
  (section share)
  (package ui-server))